
export default function DisplayInfo(item) {
    
    // console.log(item.predictions);

    return (<>
        <h1>PREDICTIONS</h1>
        {/* <h1 style={{paddingTop: '5rem'}}>{item.predictions.attributes.title.nodeValue}</h1> */}
    </>)
}